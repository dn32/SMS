<Window x:Class="SMS.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMS.Viewer"
        mc:Ignorable="d"
        WindowStyle="none"
        ResizeMode="NoResize"
        Name="MainWindow1"
        Topmost="True"
        Height="15" 
        Width="365"
        AllowsTransparency="True"
        ShowInTaskbar="False" 
        Background="Transparent" 
        MouseLeftButtonDown="GridOfWindow_MouseLeftButtonDown"
        MouseDoubleClick="MainWindow1_MouseDoubleClick"
    >

    <Border CornerRadius="2" BorderThickness="0">
        <Grid Margin="0,0,0,0" >
            <TextBlock Text="Carregando..." Name="TxtValor" TextAlignment="Center" FontSize="11" Margin="0,0,0,0" Foreground="Black"/>
        </Grid>

        <Border.Background>
            <SolidColorBrush Opacity="0.3" Color="White"/>
        </Border.Background>
    </Border>
</Window>
